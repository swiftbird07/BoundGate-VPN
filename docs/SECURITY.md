# Security: threat model and risk register

Living document. Every milestone updates the register.

## Assets

* Node identity keys (in hardware; softkey only for development).
* The registry: which keys are approved with which roles and prefixes,
  which sessions exist.
* The networks behind hubs, subnet routers and exit nodes, and the overlay
  itself (node-to-node reachability).
* Flow logs (contain user, node, destination data).

## Threat model

### What we defend against

| Threat | Defense |
|---|---|
| Copy of the node's state directory to another machine | Key is non-exportable (TPM/SE); with softkey this is **not** defended, softkey is dev-only and reported as `hardware_bound=false` for policy |
| Stolen OIDC tokens | Tokens exist only for the duration of the code exchange inside the control plane; nothing token-like reaches a node. A session is a control-plane row bound to a node id and only ever travels inside snapshots; a hub honours it only on that node's mTLS connection |
| Session replay from another device | Impossible by construction: the session names a node id, the tunnel proves the node's key |
| Login started by one node, completed for another | `state` binds the callback to the flow's node; the flow id is readable only by that node; the code is single use with PKCE and nonce |
| Unknown node connecting to a hub | Registry lookup inside the TLS handshake, on every connection: no server issues session tickets, because a resumed TLS 1.3 handshake proves the ticket and not the device key, and skips the lookup (`TestServersNeverResumeASession`; before 2026-09-25 servers did issue them, see R116). Nothing above TLS exists for unapproved keys |
| Fake hub (anyone with the public address) | The spoke pins the hub's SPKI from the snapshot; a hub is a node whose key carries an admin-signed binding with the hub role |
| Control plane promotes a node to hub or widens a router's prefixes (bug or compromise) | Roles, prefixes and overlay address are in the signed binding; a record that differs from its binding is ignored by every node (tested in the lab with a direct database edit) |
| Fake control plane (DNS, rogue CA in the system store) | The node channel pins the control plane's own key, not WebPKI; a different key is refused until an operator re-pins |
| Admin session hijacked (bootstrap token, later cookie) | Can confirm and mint sign tokens, but not approve: the signature needs the admin's hardware key. The sign step re-checks the fingerprint the admin typed |
| Node revoked while up | Every hub closes its tunnels on the snapshot diff (measured: ~7 ms); reconnect fails at the handshake; the node itself loses its snapshot and tears down |
| Spoofed source addresses inside a tunnel | Hubs and receiving peers drop packets whose source is neither the node's overlay address nor inside one of its granted prefixes; an address of the overlay pool only ever belongs to its node, and inside a granted prefix a more specific network someone else announces (the hub's own LAN, a router's LAN) is not the sender's (R117). connect-ip-go enforces assigned addresses on both ends of the QUIC path; the TCP fallback relies on the handlers' check |
| A node announcing networks it was not granted | Prefixes are granted by an admin and only then assigned; requested prefixes are shown as claims |
| Profile asking for more networks than allowed | Effective routes = profile ∩ advertised; hubs also enforce the ACL per flow (M3) |
| Control plane unreachable for long | Snapshots carry `max_age_seconds`; a stale snapshot admits nobody (fail closed), and the flows that were open when it went stale are closed |
| Policy-layer bug (ACL, OIDC, SPA) | Cannot mint identity; bounded by the TCB rules in `TCB.md` |
| Replay of connection setup | 0-RTT disabled, session tickets disabled on clients and servers |

### What we do not defend against (accepted, documented)

| Limitation | Notes |
|---|---|
| Root on the enrolled node while it is enrolled | Root can use the TPM as a signing oracle and the machine as a proxy. The key cannot be extracted; access ends with revocation. |
| Enrollment of a software key on a device that has a TPM | No attestation in the prototype. The admin approves after comparing the fingerprint out of band and is expected to know the device. `hardware_bound` is what the node reports, not proven, and not part of the binding. |
| First contact of a node with the control plane | The control-plane key is pinned at the first enrollment, and only after the person at the device accepted its fingerprint (`boundgatectl enroll` asks, the Mac app asks; the admin UI shows the fingerprint on the Nodes page). Nothing pins silently: until then the node refuses the connection before sending its own certificate. The admin key list is then taken on first use over that pinned channel. A user who accepts without comparing, or a script with `-accept-new-pin`, is back to plain trust on first use: an attacker on path at that moment can substitute both. Enterprise: provision `control.pin` and `signers_genesis` by configuration. |
| Admin keys are fixed at enrollment | A key added later cannot sign for older nodes; a compromised admin key keeps verifying on nodes that pinned it. Both mean re-enrolling nodes. Chosen over an updatable list because an updatable list would again be something the control plane controls. |
| Compromise of a hub | The hub terminates tunnels and sees overlay packets between spokes by design (M7 adds end-to-end paths). Its role is admin-granted. |
| Compromise of a subnet router | Its LAN is exposed to the overlay as granted; the LAN itself is not BoundGate-authenticated (an Apple TV has no key). |
| Compromise of the control plane | Can delete nodes (deny service), change unsigned fields (name, hub public address), policy (M3) and **user sessions** (it can attach any user to any interactive node; the ACL then trusts that user), and route traffic through a chosen *approved* hub. It cannot make an interactive node a workload (kind is signed). Cannot forge a device key, invent a node, change roles or prefixes, or move an overlay address: those are in the admin-signed binding that every node verifies against keys pinned at enrollment (`BINDINGS.md`). Cannot impersonate the control plane to enrolled nodes either (pinned key). |
| TPM firmware vulnerabilities | Out of scope; re-enroll all nodes if one becomes known. |

## Risk register

| # | Risk | Status | Mitigation / decision |
|---|---|---|---|
| R1 | connect-ip-go is pre-1.0 (v0.2.0 pinned, v0.3.0 blocked by cooldown until 2026-09-22) | open | All use isolated in `internal/transport`; `Tunnel` wrapper; pin exact versions |
| R2 | http3 exposes the QUIC connection only via `ConnContext` | verified M0 | `ConnContext` runs after the handshake; `PeerFromTLSState` re-verifies per request |
| R3 | Enroll endpoint is reachable without approval | mitigated M1, bounded 2026-09-25 | Creates `pending` rows only; rate limit 5/min per client (the address for IPv4, the /64 for IPv6, which one host can fill with addresses); at most 500 requests pending, beyond that 429 until an admin cleared some or they expired; expiry 24 h; fingerprint shown prominently. Every value in a request (roles, prefixes, public address, hardware claim) stays a request that only an admin grants |
| R4 | Admin approves without comparing the fingerprint | partly M1 | API takes the confirmed fingerprint and refuses mismatches; UI (M4) will require it; procedure in ENROLLMENT.md |
| R5 | Cedar schema validation not used; attribute typos only show up at evaluation time | accepted M3 | Policies are parsed on save; `POST /admin/acl/evaluate` dry-runs on live state and returns evaluation errors; the flow log records them per flow |
| R6 | MTU/fragmentation over QUIC datagrams | mitigated 2026-09-22 | TUN MTU 1280 (the IPv6 minimum, `transport.FitMTU`) both ends. Tunnels send QUIC packets of 1340 bytes from the first one on (`transport.PacketSize`), because a connection behind boundgate-mux never grows its packets by path MTU discovery: with quic-go's 1280 only 1230 fit (2026-09-20), and with 1230 the Mac's kernel dropped the 1250-byte-and-larger QUIC packets of Safari and Google with DF set before the tunnel, so large pages over HTTP/3 stalled (2026-09-22). A path that cannot carry 1368 bytes fails the QUIC handshake and `transport: auto` goes to TCP/443. A packet that still does not fit is answered with ICMP "fragmentation needed, MTU 1280", not with connect-ip-go's "packet too big" Since 2026-09-21 every node also clamps the MSS of TCP SYN and SYN-ACK packets it lets pass to its tunnel MTU minus 40 (`netparse.ClampMSS`, in `session.admit`): a hub at a smaller MTU makes connections of a client at a larger one fit, and servers that send without the DF bit (whose packets the hub's kernel would fragment) never send a segment that needs it. Found on pVPN: hub at 1230, Mac at 1280, `www.heise.de` without DF: every full segment arrived as two fragments and the second was dropped (R101). |
| R7 | Colima kernel features (tun, nftables, ip_forward) | verified M0 | Works on kernel 6.8 in Colima |
| R8 | Bypass routes go stale when hub/control/IdP IPs change | open | Re-resolved on every dial; DNS changes mid-session are not tracked |
| R9 | macOS node needs root for utun and modifies `scutil` DNS | M5 | launchd daemon, state file, cleanup on next start |
| R10 | Secure Enclave cannot be used from a launchd daemon | design | User-context helper process signs on behalf of the daemon over local IPC (post-prototype) |
| R11 | Authentik `groups` claim needs a scope mapping | M2 | Documented configuration; mockoidc for CI |
| R12 | Loss of the only admin passkey | M4, CLI since 2026-09-25 | On the control-plane host: `boundgate-control -config … rotate-bootstrap-token -revoke-passkeys` revokes every passkey (audited, actor `host`) and writes a new bootstrap token to the token file (0600); the old token is dead. The first admin to sign in registers a new passkey with it. Without `-revoke-passkeys` it only rotates the token, which then works only if no passkey is active. Needs what reading the database needs anyway: access to the host (ADMIN-AUTH.md) |
| R13 | Nodes shell out to `ip`/`nft` | accepted | Small, auditable commands in the system log; netlink later if needed |
| R14 | Softkey used in production | policy | `hardware_bound=false` visible in registry, UI and ACL context; policies should require `hardware_bound` |
| R15 | Chat exports in the repo directory contain ChatGPT session tokens | mitigated | `*.webarchive` and the HTML export are in `.gitignore`; never commit |
| R16 | Registry file replaced atomically is invisible through a single-file bind mount | closed M1.5 | The standalone registry file no longer exists; nodes always use the control plane |
| R17 | `go test -race` unavailable in the dev container (no cgo/gcc) | fixed M0 | gcc added to the box image; `make test-race` |
| R18 | Admin API with a bearer token | accepted dev | Now TLS on 443 (self-signed in dev, WebPKI in production), bound to 127.0.0.1:18443 in compose; M4 replaces the token with OIDC + passkey sessions |
| R19 | Nodes trust the control plane's node-channel certificate from a shared file | closed M1.6 | Nodes pin the SPKI (TOFU into `control.pin`, or `control.pin` in the config); a changed key is refused (tested in e2e) |
| R20 | Gateway token file on disk | closed M1.5 | Gateways are nodes; the node channel is authenticated by the device certificate, there is no token |
| R21 | Hubs terminate tunnels and see overlay traffic | accepted, reduced M7 | Hub role granted by an admin (signed in M1.6); payloads are mostly TLS anyway. Between spokes that both take part the traffic runs on an end-to-end path (PATHS.md) and the hub sees ciphertext or nothing; the hub path remains for everything else and as the fallback, so a hub can still force traffic onto itself by refusing to relay |
| R22 | Verifier bug: a wrong SPKI or binding accepted | mitigated M1.6 | One implementation each in `transport` (`verifyDevice`, `verifyPinned`, `serverKeyHash`) and `binding` (`Parse`, `Matches`, `Verify`); negative tests (tampered field, foreign signer, copied signature, sk/software key confusion, non-canonical JSON), fuzzers for the SSHSIG and binding parsers; AST test for foreign constructors still open |
| R23 | Loss of all admin signing keys | documented | Several keys from the start, any one signs; keys can be added and replaced at any time with a signature of a current key (signed list, `BINDINGS.md`); only losing **all** keys of the current list means re-enrolling every node |
| R24 | Admin signing keys are delivered to nodes at enrollment by the control plane | accepted | TOFU over the pinned channel, once; afterwards the list only moves along links signed by one of its own keys (R83); enterprise provisions the genesis hash (`control.signers_genesis`); enrollment refused while no key exists so nobody pins an empty set |
| R25 | Routed mode without a return route in the LAN; overlapping home networks | documented | Mode per prefix, `snat` as fallback; Fritz!Box static routes documented in DEV.md; 1:1 NAT later |
| R26 | Snapshot too old when the control plane is down | mitigated M1.5 | `max_age_seconds` (default 24 h) then fail closed; `Holder.Stale` in the handshake lookup; since 2026-09-25 the flow sweeper also decides every open flow again when the snapshot turns stale, which closes them (tunnels stay up and carry nothing a policy allowed) |
| R27 | SSHSIG framing implemented by us | mitigated M1.6 | Format from OpenSSH PROTOCOL.sshsig; test vector made with `ssh-keygen -Y sign`, and our ed25519 signature over it is byte-identical to ssh-keygen's; signature algorithm pinned to the key type; x/crypto/ssh verifies (incl. sk user-presence flag) |
| R28 | One-time token for the CLI signing step | mitigated M1.6 | 10 min, hash stored, bound to node id and key hash, marked used in the approving transaction (reuse = 409, tested), older unused tokens of the node invalidated, rate limited, audited; the token travels in the confirm response and in a shell command line (history), acceptable for a 10-minute single-use secret |
| R29 | Admin and node traffic share port 443 | mitigated M1.5 | SNI decides the TLS configuration; the node name requires a client certificate, the admin name never does; tested (`TestSNISplit`) |
| R30 | A spoke hangs on several hubs; asymmetric paths | accepted | One primary per spoke for all destinations, hub addresses via their own link; switch only when the tunnel ends; hubs do not forward for each other (a spoke connected to only one hub can be unreachable through the other) |
| R31 | Hub crash (not graceful stop) is noticed only after the QUIC idle timeout | accepted | Idle timeout 30 s, keepalive 10 s on tunnels; graceful stop closes tunnels immediately (measured failover 0 s) |
| R32 | Node-channel HTTP/3 falls back to TCP on any dial error | accepted | Same TLS identity and SNI on both; fallback is logged and retried on H3 every 5 min |
| R33 | Signed binding does not cover name, public address, `hardware_bound` (the latter signed since M6: R67; tags are signed: R100) | accepted; public address admin-only since 2026-09-25 | The name is not security-relevant. The public address is: every peer routes it outside the tunnel (R97), so it is only ever what an admin set, never the node's enrollment value, and it must be host:port; a wrong hub address fails the pinned handshake. `hardware_bound` is a claim anyway (no attestation); policies must not treat it as proven |
| R34 | Control plane re-verifies nothing at snapshot time | accepted | It verifies at signing and stores bytes; nodes verify on every snapshot, which is the check that matters. A consistency check at build time would only catch bugs, may come later |
| R35 | Dev tooling in the control image (`ssh-keygen`, `sqlite3`, `boundgatectl`) | dev only | Documented in `Dockerfile.control`; a production image carries only `boundgate-control` |
| R36 | Node's own binding invalid → holder cleared → hub admits nobody | accepted (fail closed) | A hub whose record the control plane tampered with stops serving; the alternative (keep running on the old snapshot) would let a tampering control plane keep a node in a state it chose |
| R37 | Node-channel key rotation means re-pinning every node | documented | Same for admin keys; deliberate, see `BINDINGS.md` |
| R38 | OIDC callback is unauthenticated and on the public admin name | mitigated M2 | State (24 random bytes) binds it to a flow; flows expire in 10 min; code single use; PKCE + nonce; errors are generic; audited in `user-auth` |
| R39 | Session lifetime is a fixed 10 h, no revocation on IdP side changes | accepted | The control plane never talks to the IdP after login; admins revoke sessions in BoundGate. Authentik logout/disable does not end a BoundGate session before its lifetime. Back-channel logout later |
| R40 | Subnet routers cannot enforce sessions per peer | accepted | They see packets from the hub tunnel; the hub enforces before forwarding, the ACL (M3) decides per flow with the user |
| R41 | Session expiry between snapshots | mitigated M2 | Hubs check `expires_at` every 10 s and at Accept; the control plane sweeps every 15 s and bumps; a stale snapshot admits nobody |
| R42 | The fake IdP and its default secret in the lab | dev only | `boundgate-fakeidp` logs anyone in; only reachable on the compose network; production config uses Authentik with a secret file |
| R43 | A router identifies the principal by source address, not by a handshake (hub path; on a path between spokes the receiving node did the handshake itself, M7) | accepted M3 | The hub verified that the source belongs to the peer (`allowedSource`) before forwarding, and the hub role is admin-signed; a compromised hub could already read and inject traffic (R21). Until 2026-09-25 `allowedSource` let an exit node (0.0.0.0/0) send from any address, other nodes' overlay addresses and the hub's LAN included, so that the nodes behind the hub attributed its packets to them (R117) |
| R44 | SNI is only known after the TCP handshake; the first packets of a forbidden TLS connection have already been forwarded | accepted M3 | Only the SYN/ACK exchange passes; the ClientHello that reveals the name is dropped and both ends get RSTs; no application data reaches the server. ECH hides the name entirely (then only IP/port policies apply) |
| R45 | Verdict cache: a flow allowed under an old policy or session keeps running | mitigated M3 | Every snapshot re-evaluates all open decided flows and closes the ones no longer permitted; a peer's removal closes its flows; hubs close the tunnel on session end |
| R46 | Flow-table exhaustion by a peer opening many 5-tuples | mitigated M3, quotas 2026-09-25 | Bounded table (65536), new flows dropped above it (fail closed) and counted. One peer holds at most 8192 flows, the node's own host stack and LAN together 16384, so nobody takes the table from the others; a TCP flow the other side never answered expires after a minute instead of 30; split ClientHellos buffer at most 32 MiB together (above that a flow reads as one without a name, which a waiting server-name permit resets) |
| R47 | Shipped logs are self-reported by nodes | accepted M3, tightened 2026-09-25 | The reporter is authenticated by mTLS and its id is stamped by the control plane (`node_id`, `reported_by`); a tunnel event is recorded with the reporter as the accepting end, and cannot change a tunnel another node reported (R124); batches are bounded (2000 events, 4 MB) and, since 2026-09-25, rate-limited to 30 per minute per node. The earlier claim "cannot lie about others'" was wrong: a record names a principal, a user and a session, and a compromised node can write any of them, so it can put flows in another node's or user's name. Since 2026-09-25 the names of principal and owner come from the registry, not the record; a session is linked only when it belongs to that principal; and the Flows page shows every record under the node that reported it, with "per <reporter>" beside a principal other than the reporter. What remains: the flows of a principal are that reporter's account of them, and whether a flow happened only the two ends know |
| R48 | Asymmetric hub paths see the responder as principal | open | Documented in ACL.md; permit both directions or keep both ends on one primary hub; M7 direct paths remove the hub from the middle |
| R49 | A policy that does not compile on a node | mitigated M3 | The control plane refuses to store it; a node that still receives one (version skew) skips it, logs it and shows it in `boundgatectl status`; it never widens access |
| R50 | Denied flows are logged once per 10 s per 5-tuple | accepted M3 | Prevents log floods from port scans; the counter of denied flows is in the node status. Since 2026-09-25 what libraries write with the standard `log` package (connect-ip-go logs every packet it refuses) passes at most 20 lines per 10 s, and every stream file is rotated at 256 MiB with three old files kept, so a peer cannot fill the disk through the log |
| R51 | Admin session cookie theft (XSS, malware on the admin's machine) | mitigated M4 | `HttpOnly`, `Secure`, `SameSite=Lax`, 1 h lifetime (8 h lab), CSP `default-src 'self'` on the SPA, no inline scripts; every privileged action is audited with the session's subject; logout revokes server-side. A stolen cookie is still a full admin for its lifetime: shorten `admin.session_lifetime` where that matters |
| R52 | CSRF against cookie-authenticated mutations | mitigated M4 | `SameSite=Lax` plus the mandatory `X-Requested-With: BoundGate` header on non-GET (tested); bearer tokens carry no ambient authority |
| R53 | OIDC alone would be enough for an attacker who phishes the IdP login | mitigated M4 | The session stays `oidc_only` (can only run passkey ceremonies) until a WebAuthn assertion with user verification succeeds; phishing-resistant by origin binding (`rp_id`) |
| R54 | The first passkey (bootstrap) is the root of admin trust | accepted M4, tightened 2026-09-25 | It requires the bootstrap token from the control plane's state directory (file system access) *and* an OIDC login in the admin group; the token is dead afterwards. The token is written only to a 0600 file (`bootstrap_token_file`, default `bootstrap-token` next to the database); until 2026-09-25 it went into the system log when no file was configured. API tokens minted with the bootstrap token were full admin tokens that outlived it; now they are marked, revoked when the first passkey is registered and refused while a passkey is active. Rotate with `boundgate-control rotate-bootstrap-token` (R12) |
| R55 | A pending passkey approved by the wrong admin | mitigated M4 | Only `full` admins approve, never for themselves; the approval is audited with both subjects. The approver should verify out of band that the requester registered it |
| R56 | API tokens are bearer secrets without a second factor | accepted M4 | Only their hash is stored; creation and revocation are audited; expiries are configurable and the UI defaults to 30 days; treat them like SSH deploy keys |
| R57 | Open redirect through `next=` after login | mitigated M4, tightened 2026-09-25 | `next` must start with a single `/`; until 2026-09-25 that was all, and `/\evil.example` or `/%09/evil.example` passed, which browsers read as `//evil.example`. Now a backslash, a control character or whitespace anywhere (also percent-encoded in the path) gives `/`, and the value must parse as a URL without scheme or host; the stored value is checked again at the redirect (tested with those vectors). The callback checks the flow cookie so a captured callback URL cannot finish a login in another browser (tested) |
| R122 | The admin login entry point stores a flow per request without authentication, and nothing deleted expired flows or ended admin sessions: anyone could grow the database | mitigated 2026-09-25 | 10 logins per minute per client (the /64 for IPv6), at most 1000 flows waiting for the IdP (then 503), and the housekeeping loop deletes expired admin flows and admin sessions that ended a day ago every minute; ended user sessions are deleted after `log_retention`. The other expiring tables (user login flows, sign tokens, signer list proposals, pending enrollments, logs, tunnels) were already swept |
| R58 | The policy builder generates Cedar the admin did not intend | mitigated M4 | The generated Cedar and an English summary are shown live; the sanity check evaluates a sample connection with the real engine against the unsaved draft; storing still validates; the raw text remains editable |
| R61 | macOS node keeps its device key as a file (`softkey`) | accepted M5 | Root-only state directory; the binding carries `hardware_bound: false`, so policies can exclude such nodes (`principal.hardware_bound`). Secure Enclave keys are future work |
| R62 | Stale bypass host routes after a crash pin a hub or the control plane to an old gateway | mitigated M5 | Network journal (`netstate.json`): the next start removes what the dead process left; launchd `KeepAlive` makes that next start prompt. Tested with a fake configurator and by `deploy/macos/e2e.sh` (kill -9) |
| R63 | `hub_addrs` lets local configuration redirect where a hub is dialed | accepted M5 | The hub is authenticated by the pinned key from its admin-signed binding; a wrong address can only fail. The node configuration is root-owned, and root can already change every route |
| R64 | A second VPN on the Mac captures the hub or control-plane path | mitigated M5 | A bypass target that currently routes through another `utun` is refused with an explicit error instead of being pinned into that tunnel; overlapping prefixes are a profile matter (R25) |
| R65 | Lab UDP-over-TCP bridge in front of the hubs | accepted dev | Lab only, loopback only, sees QUIC ciphertext; exists because Colima's default forwarder drops UDP. Not part of any production path |
| R66 | The overlay pool or an announced prefix overlaps a network the machine already uses (home LAN, a second VPN); the more specific route would silently take that traffic | mitigated M5 | Overlap guard in the node: pool conflict refuses `up` before any change, a conflicting prefix is skipped and shown (`not routed`), `allow_overlap` is an explicit opt-out; `/0` and `/1` exempt. Unit-tested (`TestConflictWith`). `deploy/macos/netbackup.sh` snapshots, diffs and repairs the Mac's routing state. Not a security boundary; real collisions need 1:1 NAT (R25) |
| R67 | `hardware_bound` rests on the node's own report: no TPM attestation (EK certificate, credential activation) | accepted M6 | The report grants nothing: an admin grants `hardware_bound` at confirm and signs it into the binding, so neither the node alone nor the control plane can set it. Granting without a report is refused. The report is only the starting value of the first confirm: confirming a confirmed node again (a new sign token, "Show sign command") keeps what the admin decided, so a refusal is not undone behind their back (it was until 2026-09-25; the signature step still showed the value). Remote attestation is future work; vTPMs and swtpm have no manufacturer chain anyway (TPM.md) |
| R68 | vTPM: the hypervisor admin can copy the vTPM state with the VM (clone, backup) | documented M6 | Protects against guest-level theft only; TPM.md says so per TPM type, the admin can decline `hardware_bound` for such nodes, clones must enroll with a fresh state directory |
| R69 | TPM key without password or policy session; TPM bus unencrypted | accepted M6 | Root on the machine can use (not extract) the key while it has the machine, as with any unattended machine key; the user factor is OIDC. Parameter encryption against bus interposers is future work; irrelevant for fTPM/vTPM |
| R70 | Raw socket TPM: the daemon flushes all transient objects at start | accepted dev | Only for `unix:`/`tcp:` devices (swtpm), which belong to one node; never done on `/dev/tpmrm0` |
| R71 | TPM cleared or mainboard replaced: the node identity is gone | documented M6 | By design (no export, no backup of the key); the node enrolls again, the old node is revoked |
| R72 | The daemon socket is open to the Mac's `admin` group (app without sudo) | accepted M8 | Administrators can become root anyway; standard users and other groups get `EACCES`. What the socket offers is limited to this node's own VPN: no file access, no command execution. Servers keep `root` only (no `socket_group`) |
| R73 | The control plane address comes from the user, and its key is pinned on first use | documented M8 | The key is pinned only when the user accepts the fingerprint: the app shows what the control plane presents and enrolls after "It matches", `boundgatectl enroll` asks the same (2026-09-20; before that it pinned silently at the first status poll). What remains is a user who does not compare. `configure` is refused once an address is set; `reset` (only while down) removes address, pin and admin keys together, never one without the others. A configuration file that names the control plane cannot be overridden from the socket |
| R74 | Root daemon inside an app bundle that administrators can modify | accepted M8 | Same trust level as R72 (an admin is root-equivalent). The bundle is signed with the hardened runtime; a Developer ID build is notarized. Stage 2 (system extension) moves the code out of the user-writable bundle |
| R75 | The app opens the login URL it gets from the daemon in the browser | mitigated M8 | Only `https`/`http` URLs are opened; the URL comes from the pinned control plane over the node channel. The app never sees credentials: sign-in happens at the IdP in the browser |
| R76 | Built-in ACME client for the admin name (autocert, TLS-ALPN-01); control plane and hub share a host | accepted M8 | ACME only touches the browser-facing certificate; the node channel keeps its own pinned key and is never served the ACME certificate (tested: `TestSNISplitWithACMECallback`). Host whitelist = `server_name` + `extra_names`. A host that runs both the control plane and a hub concentrates R21 and the control-plane risks in one machine: acceptable for a small deployment, separate them otherwise; never share one UDP port between them (DEPLOY.md) |
| R77 | `boundgate-mux` parses unauthenticated packets from the internet (TLS ClientHello, QUIC Initial) in front of control plane and hub | mitigated M8 | It holds no key and cannot authenticate anyone, so it is outside the TCB: a bug or a hostile mux can deny service or misroute, and a misrouted handshake fails at the pinned key (control plane) or the signed binding (hub), tested. Parsers are bounds-checked and fuzzed (`FuzzRouteUDP`), reassembly is capped (8 KiB, 4 datagrams, 8192 handshakes, 15 s), TCP peeks have a deadline; it runs unprivileged with only `NET_BIND_SERVICE` on a read-only root |
| R78 | The header between mux and server (client address) is not authenticated; the first byte of a connection ID reveals which server a connection belongs to | accepted M8 | Servers accept the header and PROXY protocol only from `behind_mux.trusted` and listen on loopback; an on-path observer can tell hub from control-plane traffic, which the server name in the Initial packet reveals anyway |
| R79 | Servers run whatever `gitlab.net407.com/sbh/boundgate:latest` is; the registry, the CI runner and the Gitea account that can push are in the TCB of every deployment | accepted M8 | Image built only by CI from `main` (or `make image-push` from a developer machine with a `write:package` token) from cooldown-checked dependencies; each build also tagged `sha-<commit>` so a server can pin (`BOUNDGATE_IMAGE` in `.env`) and roll back; pulls over HTTPS with registry login; no image signing or SBOM yet |
| R80 | The tunnel's TCP fallback adds a second listener and a hand-written capsule parser to every hub; an attacker who can block UDP can force clients onto it | mitigated M8.2 | Identical mTLS: the TLS handshake with the device certificate and registry lookup (`ServerTLSConfig`) gates the TCP path exactly as it gates QUIC, tested for unknown and revoked devices; the HTTP layer sees only authenticated peers. Capsule lengths are capped (64 KiB datagram, 1 MiB control), reads have the idle deadline, unknown capsules are skipped per RFC 9297. Forcing TCP degrades performance (TCP-in-TCP), not security, and the spoke reports it (`over TCP`); `tcp_fallback: false` removes the listener where UDP is guaranteed. Since 2026-09-25 packets to a TCP tunnel go through a queue of 256 with its own writer: a peer that stops reading loses packets, as on a congested QUIC path, instead of stalling the hub's single TUN reader for every other peer (before, up to the 5-minute idle timeout); closing a link aborts a pending write; idle HTTP/1.1 connections between requests end after 30 s |
| R81 | The admin UI shares the public listener with the node channel, so its login page is reachable by anyone who can reach 443 | mitigated M8 | Separate TLS configurations per server name (R29); admin access needs OIDC + admin group + passkey + CSRF header; `admin_allow` answers 403 for the admin name to addresses outside the list, per request (`adminGate`, tested). Two requests pass from anywhere: `GET /api/v1/oidc/callback` and, since 2026-09-25, the confirmation `POST /api/v1/oidc/confirm` (R120), because users sign in to their nodes from any network. They are marked, and an admin login arriving there from outside is refused (tested). Since 2026-09-25 the TCP server bounds every phase of a connection (headers 10 s, request 60 s, answer 3 min, above the longest long-poll of 120 s, idle 120 s, headers at most 64 KiB), so a client that sends or reads slowly cannot hold connections open without end; before, only the header read was bounded. Since 2026-09-22; before, the handshake itself was refused, which also broke users' sign-in from outside the list (an iPhone on a mobile network). The admin certificate is now served to anyone, which it is in the CT logs anyway. Addresses come from the front's PROXY header or the mux encapsulation, both accepted only from `behind_mux.trusted`. Certificates for the admin name can come from DNS-01 (lego service) so 443 need not be reachable by the CA; renewed files are reloaded, unparseable ones ignored with a log line |
| R82 | A mux behind a reverse proxy (`listen_tcp`, `trusted_fronts`) believes the PROXY header of that proxy: whoever can connect from a trusted address chooses the client address that control plane and hub log and that `admin_allow` checks | accepted | Addresses are never an authentication factor for nodes (mTLS, pinned keys, signed bindings); `trusted_fronts` lists single hosts, the TCP listener binds a private address the firewall opens to the proxy only; for the admin UI `admin_allow` is defence in depth behind OIDC + passkey |
| R83 | The admin key list can change after enrollment (signed chain): a flaw in its verifier, or a way around it, would hand a compromised control plane every node | mitigated | One verifier (`binding.VerifyChain`) for nodes, control plane and CLI; a new list needs a signature of a key of the **pinned** list in its own SSHSIG namespace, version = pinned+1, `prev` = hash of the pinned set, canonical form, never empty; all-or-nothing, persisted before use, damaged state is an error. Tests play the attacks (key signs itself in, stranger, removed key, fork, rollback, replay, other deployment, cross-protocol signature, mutated bytes, database write access; fuzzing asserts trust never moves without a valid signature), and the lab does it live (`e2e.sh` step 14) |
| R84 | A removed admin key stays trusted by nodes that never see the removal (offline, or a control plane that withholds it); with that key and the control plane an attacker can fork the list for those nodes | accepted | Inherent to revocation without a third party; `max_age` makes nodes without fresh snapshots fail closed, a node that followed the real chain can never be moved to the fork, the admin CLI keeps its own pin and refuses to sign on a forked history. Removing a key also invalidates the bindings it signed (nodes go back to `confirmed`), so removal is complete where it arrives |
| R85 | A Mac's `secure-enclave` key kind is the node's own report, like `tpm2`: no attestation of the key to the control plane | accepted M6.1 | Same as R67: the report grants nothing, an admin grants `hardware_bound` at confirm and signs it, and confirming again keeps that decision. Apple offers key attestation only to managed devices (MDM, ACME `device-attest-01`); future work |
| R86 | The Secure Enclave key needs no user presence, and its wrapped form lies in the state directory: root on the Mac can have it sign while he is on the machine | accepted M6.1 | By design, the daemon connects unattended (the person is OIDC's matter). The key still cannot leave the Mac, which is what `hardware_bound` claims; equals R69 for TPMs. SECURE-ENCLAVE.md |
| R87 | The Secure Enclave is reached through a separate helper program that the root daemon runs | mitigated M6.1 | Absolute path next to the daemon (or configured), refused when group/world writable, empty environment, wrapped key on stdin; every signature is verified against the enrolled public key before use, and the public key is re-checked at start. Same trust level as the daemon binary itself in the bundle (R74) |
| R88 | Updates: nodes install software they fetched from the release server | mitigated M8.3 | Only what a manifest signed by a built-in release key names, by SHA-256 / image digest; version must match the offered tag and be newer than what runs (no replay of an old signed release, no downgrade); the access token never leaves the configured host. The server is trusted with availability only. Tests: internal/update, `make update-test`. RELEASES.md |
| R89 | Who can sign a release that every node installs | reduced 2026-09-20 | The release key and the Developer ID identity exist on the maintainer's Mac only (`make release`, RELEASES.md); CI builds an unsigned draft and holds no signing key, so Gitea, its admins and its runners cannot publish an update. Remaining: the maintainer signs what CI built (Linux binaries, image) after checking it against CI's own record and the tagged commit, which does not detect a dishonest runner; a local reproducible rebuild to compare with is the way out, not done. The maintainer's Mac and the key's passphrase (or FIDO2 key) are now the thing to protect |
| R90 | The root daemon replaces the app bundle on request of anyone in the socket's group (R72) | mitigated M8.3 | The request carries no parameters: the daemon installs the latest release it verified itself, newer than itself, only while down, after bundle id, code signature, team and Gatekeeper checks; the swap is a rename with rollback. An admin of the Mac could already replace the app (R74) |
| R91 | A release key that is lost or leaked cannot be replaced remotely: the keys are compiled in | documented M8.3 | Several keys can be listed from the start (one offline as a reserve). Replacing a leaked key means a release signed by a remaining key that ships a new list; with a single leaked key, a reinstall |
| R92 | A relaying hub could drop, delay or reorder the packets of a path, or refuse to relay | accepted M7 | It cannot read or forge them: the path is its own QUIC connection with mTLS against the peer's pinned key (`TestRelayCarriesTheTunnelHandshakeOfTwoNodes`: wrong key fails, unknown node fails). Denial only moves the traffic back to the hub path, where R21 applies as before |
| R93 | The relay as an amplifier or open proxy | mitigated M7 | Only peers authenticated by mTLS reach it; targets are overlay addresses of approved nodes that asked to listen, the hub opens no socket; a node can listen for its own address only; 64 dialers per listener; datagrams are forwarded one to one |
| R94 | A spoke accepts tunnels: new listening surface on endpoints (relay streams, optionally a UDP port) | accepted M7 | Same `transport.Server` and TLS configuration as a hub (one implementation, R22): an unapproved key fails the handshake. Interactive peers need their session. Packets are taken only from the peer's bound addresses and only for this node and its own networks; nothing is forwarded into another tunnel. `paths.disabled: true` removes the surface |
| R95 | A `forbid` scoped to hubs only is no longer in the way once two spokes have a path | documented M7 | Before M7 a spoke-to-spoke flow was decided by the hub and by the receiving spoke; on a path only the receiving spoke decides, with the policies scoped to it. Unscoped policies (the default, and all the lab uses) behave as before. ACL.md says so under Scope; `paths.disabled: true` on a spoke or `relay: false` on the hubs keep the hub in the middle. The policy editor warns when a scope contains hubs only |
| R96 | Tunnel reports now come from every node, not only hubs | accepted M7 | A node can only report tunnels it accepted itself (its id is taken from mTLS), never one with itself; a lying spoke can add false edges that start at itself to the history, nothing else. The history is informational, no decision reads it |
| R97 | `public_addr` of a node is unsigned and makes every peer send UDP to it and route that address around the overlay (bypass route) | accepted M7, admin-only since 2026-09-25 | Until 2026-09-25 the address a node sent at enrollment took effect (the UI pre-filled it and an empty field kept it), so a node could pull any address, a server on a peer's LAN or a metadata address, out of every peer's tunnel. Now the node's value is stored apart (`requested_public_addr`) and only shown; the address in effect is what an admin sets at confirm or with PATCH, `""` clears it, and it must be host:port (no scheme, no path, port 1-65535). A wrong value fails the pinned handshake and the path falls back to relay or hub. A hostile control plane, or an admin who sets a wrong address, can still make peers route one address outside the tunnel and send handshake packets there |
| R121 | A router prefix that overlaps the overlay pool (10.0.0.0/8 next to the pool 10.21.0.0/16) would route overlay addresses to that router instead of through the overlay; a pool in public address space would shadow internet destinations | mitigated 2026-09-25 | Confirm and PATCH refuse a prefix that overlaps the pool, except a default route (`0.0.0.0/0`, an exit node; the node keeps pool addresses out of it itself); changing the pool is refused while it overlaps a prefix a node routes; the pool must lie in 10/8, 172.16/12, 192.168/16 or 100.64/10 and be /8 to /30. Prefixes are in the signed binding, so an admin key confirms them as well |
| R98 | Releases are read from a public mirror on GitHub (and the image from ghcr.io): a second party that can edit what updaters see | documented 2026-09-20 | The mirror holds the same signed manifest and files; nodes install only what a release key signed, by hash and digest, and nothing older than what runs. Whoever controls the GitHub repository (or Gitea) can therefore **withhold** releases, not change or roll back one: a node that is shown nothing newer stays on its version and cannot tell (freeze). Visible only as the age of the installed version in the admin UI. Nothing is built on GitHub (Actions off), no signing key is there; the maintainer's token can write to that one repository, and `mirror-github.sh` re-verifies signature and hashes before it uploads and refuses a tag that is not the one made locally |
| R99 | Hosts that follow the image tag `latest` (`docker compose pull`, Dockhand, Watchtower) and the `curl … \| sh` installer check no signature | accepted 2026-09-20 | A convenience the operator chooses: `setup.sh` asks, and recommends `update.sh`, which verifies the signed manifest on the host and pins the digest. With `latest` the host trusts ghcr.io, the GitHub account that owns the package and the CI secret that can push to it (as R79 did for the private registry); CI moves the tag only to the digest of a published release whose signature it checked, which protects against mistakes, not against someone holding the push token. `setup.sh` itself arrives over TLS from GitHub unsigned, like every such installer: it can be downloaded and read first, writes only into one directory, and with signed updates the image it starts is verified by `update.sh pin` against the release keys fetched alongside it (same origin, so this guards the registry, not GitHub) |
| R100 | Node tags select nodes in policies (`principal in BoundGate::Tag::"…"`): whoever can set a tag can widen a policy | mitigated 2026-09-20 | Tags are a field of the signed binding (BINDINGS.md): only an admin key grants them, a change demotes the node until it is signed again, every node checks its peers' tags against their signatures. `boundgatectl admin sign` shows kind, roles, tags, prefixes and address from the binding's own bytes and checks node id and key hash in them, so a control plane cannot describe one grant and have another signed. What stays with the control plane: the policies themselves, which are not signed: a compromised control plane can rewrite them (threat table, "Compromise of the control plane"); signed tags do not change that, they keep tags from being the easier way |
| R101 | IPv4 fragments other than the first carry no ports: a flow table that treats them as packets of their own either drops them (fragmented traffic never arrives; seen on pVPN) or, if a policy allows "port 0", lets them pass undecided | mitigated 2026-09-21 | A later fragment never opens a flow and is never decided by the ACL. It passes only when the first fragment of the same packet (source, destination, protocol, IP id) passed within the last 10 s on this node, counts for that flow, and is dropped when it starts inside the transport header (offset < 24, RFC 1858: it could rewrite the ports the decision was made on). The table of pending packets is bounded (4096, then fail closed). Payload inspection (SNI, DNS name) sees first fragments only; a ClientHello split by IP fragmentation is treated like one split over TCP segments. |
| R102 | A permit by TLS server name lets the TCP handshake through before the name is known: the server sees a connection from the node (the exit node's address) that is then reset, a port-scan-like trace, and a client can complete handshakes with any host the policies otherwise deny | accepted 2026-09-22 | Only when a permit in scope mentions `sni`, only for TCP, only the handshake: the first client payload is held until read and never forwarded unless the name is permitted, a server that speaks first gets a RST before its banner reaches the client, the pending flow expires after 10 s. Without such a permit the SYN is dropped as before. Names in permits are matched against the ClientHello only: a client can lie about the name (R33 already: the hub does not verify the certificate). |
| R102 | Embedded engine: app and extension share the state directory (app group container), and any code in the app can read it | accepted M8.5 | It holds no secret: device certificate, pinned control-plane key, admin key list, settings. The device key stays in the platform's key store (Secure Enclave, Android Keystore) and is only reachable through `Sign`. A process that can write the directory could swap the pinned keys, but it runs as the app and could equally drive the app; the same holds for root and a daemon's state directory. Only one engine runs on the directory at a time (`flock`). |
| R103 | Embedded engine: `KeyKind` and `HardwareBound` come from the app | accepted M8.5 | As with a daemon they are the node's claim. An admin grants `hardware_bound` and signs it into the binding. Android key attestation, shown to the admin before signing, is planned for M9b. The core only asks the platform to sign SHA-256 digests (TLS 1.3 client authentication, the self-signed device certificate) and rejects anything else. |
| R104 | Embedded engine: the app's own sockets must stay outside the tunnel | accepted M8.5 | Network extensions get this from the system, and Android excludes the app (`addDisallowedApplication`). `excluded` also lists control plane, hubs and IdP for platforms that route by address. A platform that gets it wrong loops the control channel into the tunnel and loses the connection: fail closed, no traffic leaves unprotected because of it. |
| R105 | Windows: the tray and every member of `BoundGate Users` drive the service (up, down, sign-in, enroll, configure, reset) | accepted M10 | The same trust as the Mac's `admin` group on the socket. The socket has a protected DACL: SYSTEM, Administrators and the configured group (`socket_group`), nobody else. The group is created by install.ps1 with the installing account only. Nothing on the socket reveals a key; `reset -new-identity` destroys this machine's identity, which a member could do anyway by uninstalling. State, logs and configuration under `%ProgramData%\BoundGate` are for SYSTEM and Administrators only (protected DACL set by `boundgate-node install`), because ProgramData is readable by every user by default. |
| R106 | Windows stage 1 ships unsigned programs (no Authenticode) and cannot update itself | accepted M10 | SmartScreen warns. The zip is named with its SHA-256 in the release's signed manifest, and `boundgatectl verify-release` checks that signature on any machine. Programs go to Program Files (writable for administrators only) because the service runs as SYSTEM from there. Self-installing updates wait for Authenticode, so that the service can check a signature of the files it replaces itself with. |
| R107 | Windows: `wintun.dll` is loaded by a SYSTEM service and comes from outside the release | mitigated M10 | install.ps1 accepts it only with a valid Authenticode signature of WireGuard LLC, and copies it next to the service in Program Files. Windows loads a DLL from the application's directory first, which only administrators can write to. |
| R109 | `login_passthrough` (hub): an interactive device without a user session gets a tunnel | accepted 2026-09-22, off by default | Only for Android's lockdown mode, where the browser cannot reach the IdP outside the tunnel. The tunnel carries only flows to the listed destinations (`loginPassthrough` in the flow decision, before the policies); everything else is denied, and relaying (`RelayDial`) still needs a session. The device is approved and holds its hardware key, so this is no more than it could reach from any network: list public IdP addresses and a resolver, nothing inside the LAN. When a session ends, the tunnel stays and its other flows are closed (`Reevaluate`) instead of the tunnel. The spoke reports such a hub as "login required", not as connected. |
| R110 | Hub option `dns`: the hub names the resolvers the apps use, and DNS to them bypasses the policies | accepted 2026-09-22, off by default | The list comes from the hub over its pinned, admin-signed identity (`Boundgate-Dns` header on the tunnel answer), not from the control plane, so a compromised control plane cannot redirect names. A hub already sees and routes all this traffic. The bypass is narrow: port 53, UDP and TCP, exactly the listed addresses (`offeredDNS`); without it, offering a resolver in the LAN would break names for every device that may only reach the internet. Daemons ignore the list and keep the system's resolver. |
| R111 | `reply_via_arrival` (Linux node): answers to connections from outside bypass the tunnel | accepted 2026-09-22, off by default | Only packets of connections that came in from outside to this host carry the mark (not from the tunnel, to a local address; set before Docker's DNAT). What the host and its containers start stays unmarked and goes through the tunnel, where the hub's policies decide. A process on the host could set the mark on its own sockets (`SO_MARK`, needs `CAP_NET_ADMIN`) to leave outside the tunnel: that is root on the host, who could remove the routes as well. The tunnel's routes live in table 5184; rules 5180–5182 and the nftables table are removed on `down` and after a crash from the journal. `make test-arrival` checks it in a real kernel. |
| R112 | Power save (phones, `power_save`): a node polls its snapshot every 10 min, keeps its tunnels alive every 2 min, and the hub keeps an idle tunnel for up to 5 min | accepted 2026-09-24, on for ios/android | A phone learns of a revocation, a new policy or an expired session later, but it enforces none of them for others: hubs and receiving peers decide with their own snapshot and cut the phone off themselves (CloseDevice, session expiry). What reaches the phone from outside between two keep-alives may be lost until it sends again. The hub and the control plane no longer send keep-alives; nodes without power save keep theirs (10 s), so their dead-peer detection is unchanged. docs/POWER.md |
| R113 | Tunnels follow a network change by QUIC connection migration; a node answers unknown connections with stateless resets from a key in its state directory; a tunnel that swallows 10 packets over 30 s is probed from a new socket and given up on if unanswered | accepted 2026-09-24 | Migration and probes are QUIC path validation (RFC 9000 §8.2, §9): the hub answers only a peer that proves it holds the connection's keys, so nobody can move another node's tunnel. A stateless reset token is derived from `reset.key` and only ever travels inside the encrypted connection (NEW_CONNECTION_ID), so an off-path attacker cannot forge one; an on-path attacker could drop the packets anyway. `reset.key` lets nothing else be done than telling a client its old tunnel is gone. The liveness probe sends nothing while a tunnel is idle; a sender nothing ever answers is probed every 30 s and redialed after 8 moves. docs/POWER.md |
| R114 | A list may follow a URL: the control plane fetches it and the entries decide policy outcomes on every node | accepted 2026-09-24, off unless an admin sets one | Whoever can write that file can widen an allow-list or narrow a block-list, so a source belongs on a host the organisation controls (its own Git). Only an admin can set one, and it is audited. The fetch is `http`/`https` only, 30 s, at most 4 MiB, at most 10 000 checked entries, redirects only within the same host and scheme, no proxy from the environment; the control plane sends no cookies and no credentials besides the one header an admin gave for a private repository (kept, never returned, stripped from error messages, and dropped with the header when a PUT changes the URL's scheme, host or path without giving it again). A source it cannot read changes nothing: the list keeps its entries and shows the reason. A source URL is also a way to make the control plane send a request (SSRF), so the address is checked at connect time, after DNS: loopback, link-local, multicast, unspecified and the cloud metadata addresses (also IPv4-mapped and NAT64) are always refused, private ranges unless `list_sources.allow_private` in the control plane's configuration file admits them (an admin account alone cannot). Until 2026-09-25 none of that was checked and the status quoted the first line that was not an entry, so an admin could read a metadata token or an internal page through it; now the status names the line by number and length only. Residual with `allow_private`: the fetch can reach internal HTTP services, and it still learns whether they answer (status code, timing). docs/ACL.md |
| R115 | A dynamic access list decides a connection by a name the node learned from a DNS answer it forwarded | accepted 2026-09-25, off unless a list of kind `dynamic` exists | Whoever answers the DNS decides which address a permitted name points at. The node therefore believes only the resolvers it offers its spokes (`dns`) or the ones `dns_learn_from` names, and only for names a dynamic list actually holds; a device that asks any other resolver gets its answer but teaches the node nothing, so it cannot mint a mapping from a permitted name to an address it chose. What is learned is kept per peer and user session (one device opens nothing for another, one user nothing for the next), bounded (8192 addresses, 8 names each), short-lived (the answer's time to live, at least a minute, at most an hour, plus five minutes for the caches of the device) and never written to disk. A resolver that is lied to (a poisoned upstream, a compromised LAN resolver) can point a permitted name at another address; that resolver is the one the whole network already trusts for names, and the device would follow it in any case. The learned name only ever adds a way to match a list, never a permit of its own: the policies still decide. docs/ACL.md |
| R116 | TLS session tickets on the servers (hub, path listener, control plane's node name): a resumed TLS 1.3 handshake proves the ticket's secret, not the device key, and Go skips `VerifyPeerCertificate` on it | fixed 2026-09-25 | Found in the review of 2026-09-25: whoever copied a ticket and its secret off an enrolled device (root, once) could open tunnels from any machine without the TPM or Secure Enclave key for up to seven days, and a revoked device passed the handshake (the per-request lookup still refused it). Every server configuration now sets `SessionTicketsDisabled`, `transport.NewServer` refuses one that does not, and `TestServersNeverResumeASession` shows a client that asks for tickets getting a full handshake and the key check each time |
| R117 | A node whose granted prefix contains other nodes' addresses (an exit node's 0.0.0.0/0) | fixed 2026-09-25 | Found in the review of 2026-09-25: the hub's forwarding table let such a prefix catch everything without a host entry — the hub's own overlay address, its LAN, peers on other hubs — and `allowedSource` let the node send from any of those addresses, so a malicious exit node could answer for the hub (DNS, services) and the nodes behind the hub attributed its packets to others. Now an announced prefix never takes an address of the pool or of a network the hub announces itself (unless more specific), a source in the pool must be the sender's own address, a source inside a more specific network someone else announces is refused, a peer's default route is no longer installed in the hub's kernel, a snapshot with a prefix that overlaps the pool (other than a default route) is refused, and the pool must be a private range (it is not signed). A node also refuses a snapshot whose own record carries another key |
| R118 | A permit by DNS question or TLS server name does not say where the connection goes | mitigated 2026-09-25 | Found in the review of 2026-09-25: the first query of a UDP flow to port 53 decided the flow, which then carried anything in both directions, to any address; and the server-name fallback of a dynamic list opened any address, internal ones included, to whoever wrote a permitted name into a ClientHello. Now DNS flows are decided query by query, a question counts only towards a resolver the node knows (`dns`, `dns_learn_from`), what is not a plain query is decided without a name, a response passes only as the answer to a query that passed, and only such an answer is learned. The server-name fallback of a dynamic list applies to public addresses only; inside the overlay a name opens an address only after this device resolved it there, or by an address entry. An `sni` list keeps its meaning (the admin's explicit choice, R102) and ACL.md says to pair it with an address condition inside the overlay. ICMP errors ride an allowed flow only towards the sender of the quoted packet, redirects never |
| R120 | A user login can be phished: whoever holds an approved node starts a login and sends its link to someone else, whose IdP session signs them in without a prompt, and their identity and groups land on the attacker's node | mitigated 2026-09-25; residual: a person who confirms despite the warning | The callback does not complete a user login. It keeps the verified identity on the flow and shows a page naming the device about to be signed in (name, hostname, platform, approved since, start of the key fingerprint) with the warning "Only continue if you started this sign-in on this device yourself. Whoever holds this device gets your access."; the node sees `pending` until the person presses **Sign in this device**, which posts a single-use token bound to the flow (hash stored, 5 min, not guessable by another site, page not frameable). **Cancel** fails the flow. When the browser's address (IPv6: its /64) is not the one the node started from, the page adds a stronger warning with both addresses; it does not refuse, because mobile data, IPv6 and VPNs differ legitimately. Question and answer are both in the user-auth log with both addresses. A node keeps at most three flows open and two status polls waiting. What the page cannot prevent is a person who reads it and confirms anyway: the link is still sent from someone they may trust. docs/OIDC.md |
| R123 | What a node's snapshot tells it about the rest of the network: every approved peer, every active user session (subject, email, username, groups) and, until 2026-09-25, every list | lists mitigated 2026-09-25; sessions accepted | A node receives only the lists its policies (enabled, scoped to it) name; the admin's global view has all of them. Sessions still go to every node: any node can be the receiving end of a hub tunnel, a direct or a relayed path (PATHS.md), decides a flow by the user behind the peer (groups, subject, email in the policies) and refuses an interactive peer without a session, and which peer will connect is not known in advance. Filtering by policy would not help either, since a policy that names a group needs every session of that group. So whoever controls one node's state directory sees who is signed in on which device, with their groups; that is the directory of the organisation, not a credential, and the sessions cannot be used from another node (they name the node id, the tunnel proves the key) |
| R124 | A tunnel report names the tunnel by an id the reporting node chose: until 2026-09-25 any approved node could report another hub's tunnel id and close it, reopen it or inflate its counters in the connection history | mitigated 2026-09-25 | A tunnel row belongs to the hub (accepting node) and peer that first reported it; a report of the same id by another node or for another peer changes nothing and counts as rejected (logged with the reporter). The history is still what the accepting nodes say about their own tunnels (R47) |
| R108 | Host routes for control plane and hubs follow the best physical route when they are set; after a change of network (another Wi-Fi, a cable, another VPN) they point to the old gateway | mitigated 2026-09-22 | Every daemon watches its platform's routing events (netlink on Linux, the routing socket on macOS, IP Helper on Windows; `netcfg.Watcher`). An event only starts a look at the default routes outside the tunnel, at most every 2 s, and only when those differ (`watchDefaults`) does the node set every host route of the session again and reconnect (`Node.RoutesChanged`). Containers, ARP entries and the node's own routes change nothing there, so a Docker host is not reconnected by every container start. The route choice on Windows (`bestRoute`) is tested in the box. Embedded nodes get the same from the app (`bg_network_changed`). A route that is still wrong loops the control channel into the tunnel and fails closed. |
| R60 | Lab devproxy: the admin name as plain HTTP on 127.0.0.1:18080, cookies without `Secure` for that origin | accepted dev | Compose only, bound to loopback; exists because browsers refuse passkeys behind a self-signed certificate. The relaxation is driven by an explicit `http://` entry in `admin.origins`; production lists only `https://` origins and has no proxy |
| R59 | WebAuthn implementation (go-webauthn v0.18.0) and the CBOR/COSE parsers are in the admin TCB | accepted M4 | Pinned; attestation is not requested (`none`), so the metadata/attestation code paths stay unused; a software-authenticator test pins the expected ceremony. go-webauthn leaves decisions to the relying party: a signature counter that went backwards only sets `CloneWarning`, which the control plane turns into a refused login since 2026-09-25 (before, it was ignored, contrary to ADMIN-AUTH.md; tested). A session raised to `full` by a passkey gets a new id; the `oidc_only` id is revoked (session fixation, tested) |
| R11 (update) | Authentik `groups` claim; which claims can be trusted | documented M2, tightened 2026-09-25 | Scope mapping in `OIDC.md`; missing groups mean an empty group list, never a failure; groups are de-duplicated. Since 2026-09-25 an `email` the IdP marks `email_verified: false` is dropped, so a user cannot match a policy on an address they only typed into their profile. `username` (`preferred_username`) is often editable by the user: it is for display, and OIDC.md and ACL.md say to decide by `subject` or groups |
